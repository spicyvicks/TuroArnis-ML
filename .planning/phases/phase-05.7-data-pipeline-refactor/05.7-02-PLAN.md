---
phase: 05.7-data-pipeline-refactor
plan: 02
type: execute
wave: 2
depends_on: [05.7-01]
files_modified:
  - hybrid_classifier/models/model_front.pth
  - hybrid_classifier/logs/training_front.log
autonomous: false
requirements:
  - DATAPIPE-03
must_haves:
  truths:
    - "Front viewpoint model is trained to convergence (up to 150 epochs or early stop)"
    - "Validation accuracy >= 70% OR accuracy 60-70% (retry eligible) OR < 60% (investigate)"
    - "Model file saved with proper checkpoint format"
  artifacts:
    - path: "hybrid_classifier/models/model_front.pth"
      provides: "Trained front-view model weights"
      min_size_mb: 0.5
    - path: "hybrid_classifier/logs/history_front.json"
      provides: "Training history with loss/accuracy curves"
  key_links:
    - from: "train_features_front.pt"
      to: "model_front.pth"
      via: "4c_train_hybrid_gcn_v2.py --viewpoint front"
      pattern: "train_hybrid_gcn.*viewpoint.*front"
---

<objective>
Train HybridGCN model for the FRONT viewpoint to achieve 70%+ validation accuracy.

Purpose: Build a specialized model that only sees front-view training samples, avoiding the confusion that plagued the merged model approach.

Output: Trained model checkpoint (model_front.pth) and training history.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-7-training-pipeline-per-viewpoint/PLAN.md

## Gate 3 Criteria (from PLAN.md)
**Decision Thresholds:**
- **PASS (>= 70%)**: Continue to left model training
- **RETRY (60-70%)**: Adjust hyperparameters (LR 0.005→0.01), retrain
- **INVESTIGATE (< 60%)**: Check feature quality, debug training

**Success Criteria:**
- model_front.pth exists
- Validation accuracy ≥ 70%
- No severe overfitting (train-test gap < 20%)
- Training converged (not stopped prematurely)

## Training Command Pattern
```bash
python 4c_train_hybrid_gcn_v2.py --viewpoint front --epochs 150 --patience 15
```

If --viewpoint flag not implemented:
1. Copy train_features_front.pt to train_features.pt temporarily
2. Run training
3. Save output as model_front.pth
</context>

<tasks>

<task type="auto">
  <name>Task 1: Train front viewpoint model</name>
  <files>
    - hybrid_classifier/models/model_front.pth
    - hybrid_classifier/logs/history_front.json
  </files>
  <read_first>
    - hybrid_classifier/4c_train_hybrid_gcn_v2.py (verify --viewpoint support)
    - hybrid_classifier/hybrid_features_v3/train_features_front.pt
  </read_first>
  <action>
    Train the front viewpoint model using the prepared features:
    
    **Option A: If --viewpoint flag exists:**
    ```bash
    cd hybrid_classifier
    python 4c_train_hybrid_gcn_v2.py --viewpoint front --epochs 150 --patience 15 --output-dir models/
    ```
    
    **Option B: If no --viewpoint flag:**
    Temporarily modify the script or create a train_front.py wrapper that:
    1. Sets TRAIN_FEATURES = "hybrid_features_v3/train_features_front.pt"
    2. Sets TEST_FEATURES = "hybrid_features_v3/test_features_front.pt"
    3. Sets MODEL_OUTPUT = "models/model_front.pth"
    4. Runs training with default parameters
    
    **Expected behavior:**
    - Training will run for up to 150 epochs (or stop early if no improvement for 15 epochs)
    - Best validation accuracy will be tracked
    - Model checkpoint saved to models/model_front.pth
    - Training history saved to logs/history_front.json
    
    **Timeout handling:** Training may take 20-30 minutes. Let it complete.
  </action>
  <acceptance_criteria>
    - Process completes without errors (exit code 0)
    - hybrid_classifier/models/model_front.pth exists
    - File size > 500KB
    - Training history JSON exists with keys: epochs, train_acc, val_acc, train_loss, val_loss
  </acceptance_criteria>
  <verify>
    <automated>ls -lh hybrid_classifier/models/model_front.pth 2>/dev/null && python -c "import torch; m = torch.load('hybrid_classifier/models/model_front.pth', map_location='cpu'); print('Model loaded OK')" 2>&1</automated>
  </verify>
  <done>Front model trained and checkpoint saved (model_front.pth)</done>
</task>

<task type="auto">
  <name>Task 2: Analyze training results</name>
  <files>hybrid_classifier/logs/history_front.json</files>
  <read_first>
    - hybrid_classifier/logs/history_front.json
  </read_first>
  <action>
    Extract key metrics from training history:
    
    ```python
    import json
    with open('hybrid_classifier/logs/history_front.json') as f:
        history = json.load(f)
    
    best_val_acc = max(history['val_acc'])
    final_train_acc = history['train_acc'][-1]
    best_epoch = history['val_acc'].index(best_val_acc)
    train_test_gap = final_train_acc - best_val_acc
    
    print(f"Best Validation Accuracy: {best_val_acc:.1%}")
    print(f"Final Training Accuracy: {final_train_acc:.1%}")
    print(f"Train-Test Gap: {train_test_gap:+.1%}")
    print(f"Best Epoch: {best_epoch}")
    ```
    
    Determine status per Gate 3:
    - **PASS**: best_val_acc >= 70%
    - **RETRY**: 60% <= best_val_acc < 70%
    - **INVESTIGATE**: best_val_acc < 60%
    
    Report status in checkpoint task below.
  </action>
  <acceptance_criteria>
    - JSON file is valid and parseable
    - val_acc array contains at least 10 values (epochs completed)
    - Metrics calculated correctly (best_val_acc is max of val_acc)
    - Status classification matches thresholds
  </acceptance_criteria>
  <verify>
    <automated>python -c "import json; h=json.load(open('hybrid_classifier/logs/history_front.json')); print(f\"Best val: {max(h['val_acc']):.1%}\")" 2>&1</automated>
  </verify>
  <done>Training metrics extracted and classified (PASS/RETRY/INVESTIGATE)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify front model accuracy</name>
  <what-built>Front viewpoint HybridGCN model trained with best validation accuracy extracted from training history</what-built>
  <how-to-verify>
    1. Check the training output above for "Best Validation Accuracy"
    2. Verify the checkpoint file: `ls -lh hybrid_classifier/models/model_front.pth`
    3. (Optional) Run quick validation: `python 4d_evaluate_model.py --model models/model_front.pth --features test_features_front.pt`
    
    **Decision Matrix:**
    | Accuracy | Decision | Next Action |
    |----------|----------|-------------|
    | >= 70%   | PASS     | Approve, continue to left model |
    | 60-70%   | RETRY    | Approve but will adjust hyperparameters |
    | < 60%    | INVESTIGATE | Approve but need to debug features |
  </how-to-verify>
  <resume-signal>
    Type "PASS", "RETRY", or "INVESTIGATE" followed by the accuracy percentage
    
    Examples:
    - "PASS 73.2" - Above threshold, proceed to left model
    - "RETRY 64.5" - Need hyperparameter adjustment
    - "INVESTIGATE 42.0" - Feature or training issue
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feature file → Model | Pre-computed tensors loaded for training |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.7-03 | Denial of Service | Training timeout | mitigate | Set max_epochs=150, patience=15 to prevent infinite training |
| T-05.7-04 | Information Disclosure | Model checkpoint | accept | Model contains no PII - only pose classification weights |
| T-05.7-05 | Tampering | Checkpoint corruption | mitigate | Verify torch.load() succeeds in verification step |
</threat_model>

<verification>
- [ ] model_front.pth exists and > 500KB
- [ ] history_front.json contains valid training curves
- [ ] Best validation accuracy >= 70% (PASS), 60-70% (RETRY), or < 60% (INVESTIGATE)
- [ ] Model loads without errors
</verification>

<success_criteria>
Front viewpoint model trained with validation accuracy measured and classified for next step decision.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-05.7-data-pipeline-refactor/05.7-02-SUMMARY.md`
</output>
