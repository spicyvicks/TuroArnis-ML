---
phase: 05.7-data-pipeline-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hybrid_classifier/4c_train_hybrid_gcn_v2.py
  - hybrid_classifier/feature_templates.json
  - hybrid_classifier/train_features_front.pt
  - hybrid_classifier/train_features_left.pt
  - hybrid_classifier/train_features_right.pt
autonomous: true
requirements:
  - DATAPIPE-01
  - DATAPIPE-02
must_haves:
  truths:
    - "Training script uses Option A hyperparameters (dropout 0.5, LR 0.005)"
    - "Per-viewpoint features are regenerated for front, left, and right"
    - "Feature files have correct sample counts (~2000-2600 per viewpoint)"
  artifacts:
    - path: "hybrid_classifier/4c_train_hybrid_gcn_v2.py"
      provides: "Training script with Option A configuration"
      contains: "DROPOUT = 0.5, LEARNING_RATE = 0.005, HIDDEN_DIM = 128"
    - path: "hybrid_classifier/train_features_*.pt"
      provides: "Per-viewpoint feature tensors"
      count: 3
  key_links:
    - from: "1_extract_reference_features.py"
      to: "feature_templates.json"
      via: "template generation"
      pattern: "extract_reference_features"
---

<objective>
Prepare per-viewpoint training configuration and regenerate features for front, left, and right viewpoints.

Purpose: Set up the correct hyperparameters (Option A) and generate clean per-viewpoint feature files needed for separate model training per viewpoint.

Output: Configuration-reset training script and three viewpoint-specific feature files (.pt)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-7-training-pipeline-per-viewpoint/PLAN.md
@.planning/phases/phase-05.7-data-pipeline-refactor/5.7-01-TASK-05-SUMMARY.md
@hybrid_classifier/4c_train_hybrid_gcn_v2.py

## Key Facts from Previous Attempts
- Merged model (all viewpoints together) failed: only 24.9% accuracy
- Option A config: DROPOUT=0.5, LR=0.005, HIDDEN_DIM=128, WEIGHT_DECAY=5e-5
- Per-viewpoint training requires separate .pt feature files
- Current feature files may be outdated (from Feb 19)

## Option A Hyperparameters (per 5.7 PLAN.md Gate 1)
```python
DROPOUT = 0.5
LEARNING_RATE = 0.005
HIDDEN_DIM = 128
WEIGHT_DECAY = 5e-5
NODE_EMBED_DIM = 16
BATCH_SIZE = 32
PATIENCE = 15
```

## Files Reference
- Training script: hybrid_classifier/4c_train_hybrid_gcn_v2.py
- Feature gen: hybrid_classifier/2b_generate_node_hybrid_features.py
- Template extraction: hybrid_classifier/1_extract_reference_features.py
- Expected output: train_features_{front,left,right}.pt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reset training script to Option A configuration</name>
  <files>hybrid_classifier/4c_train_hybrid_gcn_v2.py</files>
  <read_first>
    - hybrid_classifier/4c_train_hybrid_gcn_v2.py (current configuration)
  </read_first>
  <action>
    Read the training script and verify/set these exact hyperparameter values at the top of the file (around lines 15-35):
    
    ```python
    # Option A Configuration - Per-viewpoint training
    DROPOUT = 0.5  # NOT 0.7 or 0.3
    LEARNING_RATE = 0.005  # NOT 0.001
    HIDDEN_DIM = 128
    WEIGHT_DECAY = 5e-5  # NOT 1e-4
    NODE_EMBED_DIM = 16
    BATCH_SIZE = 32
    PATIENCE = 15
    ```
    
    Also ensure the scheduler is ReduceLROnPlateau (remove verbose=True if present for Windows compatibility).
    
    Do NOT change any other logic - only these hyperparameter constants.
  </action>
  <acceptance_criteria>
    - `grep "DROPOUT = 0.5" hybrid_classifier/4c_train_hybrid_gcn_v2.py` returns match
    - `grep "LEARNING_RATE = 0.005" hybrid_classifier/4c_train_hybrid_gcn_v2.py` returns match
    - `grep "WEIGHT_DECAY = 5e-5" hybrid_classifier/4c_train_hybrid_gcn_v2.py` returns match
    - No lines contain "DROPOUT = 0.7" or "DROPOUT = 0.3"
    - No lines contain "LEARNING_RATE = 0.001"
  </acceptance_criteria>
  <verify>
    <automated>grep -n "DROPOUT\|LEARNING_RATE\|WEIGHT_DECAY" hybrid_classifier/4c_train_hybrid_gcn_v2.py | head -10</automated>
  </verify>
  <done>Training script has Option A hyperparameters (dropout 0.5, LR 0.005, weight_decay 5e-5)</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate per-viewpoint features (front)</name>
  <files>
    - hybrid_classifier/train_features_front.pt
    - hybrid_classifier/test_features_front.pt
  </files>
  <read_first>
    - hybrid_classifier/2b_generate_node_hybrid_features.py
    - hybrid_classifier/feature_templates.json (verify templates exist)
  </read_first>
  <action>
    Run the feature generation script for the FRONT viewpoint:
    
    ```bash
    cd hybrid_classifier
    python 2b_generate_node_hybrid_features.py --viewpoint front
    ```
    
    This will:
    1. Load reference templates from feature_templates.json
    2. Process dataset_split/train/front/ images
    3. Generate graph features with node embeddings
    4. Save to hybrid_classifier/hybrid_features_v3/train_features_front.pt
    
    If the script doesn't have --viewpoint flag, modify it to accept:
    - argparse argument `--viewpoint {front,left,right}`
    - Filter dataset_split/train/{viewpoint}/ and dataset_split/test/{viewpoint}/
    - Output to train_features_{viewpoint}.pt
    
    Verify output file exists and is > 2MB.
  </action>
  <acceptance_criteria>
    - File hybrid_classifier/hybrid_features_v3/train_features_front.pt exists
    - File size > 2MB (check with ls -lh)
    - File modification time is within last hour
    - Sample count in file is between 2000-2600 (print tensor shapes)
  </acceptance_criteria>
  <verify>
    <automated>ls -lh hybrid_classifier/hybrid_features_v3/train_features_front.pt 2>/dev/null || ls -lh hybrid_classifier/train_features_front.pt 2>/dev/null || echo "File not found - check output path in script"</automated>
  </verify>
  <done>Front viewpoint features generated (train_features_front.pt > 2MB, ~2000-2600 samples)</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate per-viewpoint features (left and right)</name>
  <files>
    - hybrid_classifier/train_features_left.pt
    - hybrid_classifier/test_features_left.pt
    - hybrid_classifier/train_features_right.pt
    - hybrid_classifier/test_features_right.pt
  </files>
  <read_first>
    - hybrid_classifier/2b_generate_node_hybrid_features.py
  </read_first>
  <action>
    Run feature generation for LEFT and RIGHT viewpoints:
    
    ```bash
    cd hybrid_classifier
    python 2b_generate_node_hybrid_features.py --viewpoint left
    python 2b_generate_node_hybrid_features.py --viewpoint right
    ```
    
    If the script doesn't support --viewpoint, create a simple wrapper that:
    1. Sets VIEWPOINT = "left" or "right" in the script
    2. Filters paths containing the viewpoint name
    3. Saves outputs with viewpoint suffix
    
    Verify both output files exist and are > 2MB each.
  </action>
  <acceptance_criteria>
    - hybrid_classifier/hybrid_features_v3/train_features_left.pt exists and > 2MB
    - hybrid_classifier/hybrid_features_v3/train_features_right.pt exists and > 2MB
    - All 4 files (front, left, right + test variants) exist
    - All files modified within last hour
  </acceptance_criteria>
  <verify>
    <automated>ls -lh hybrid_classifier/hybrid_features_v3/*.pt 2>/dev/null | head -10 || ls -lh hybrid_classifier/*.pt 2>/dev/null | head -10</automated>
  </verify>
  <done>All three viewpoint feature files generated (front, left, right - each > 2MB)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data loading → Feature generation | Raw images cross into processed tensors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.7-01 | Denial of Service | Feature generation | mitigate | Feature generation has timeouts; monitor for hanging processes |
| T-05.7-02 | Data Integrity | Feature templates | accept | Templates are generated from reference poses - validated during training |
</threat_model>

<verification>
- [ ] Training script constants: DROPOUT=0.5, LR=0.005, WEIGHT_DECAY=5e-5
- [ ] Front features: train_features_front.pt > 2MB
- [ ] Left features: train_features_left.pt > 2MB  
- [ ] Right features: train_features_right.pt > 2MB
- [ ] All files generated within last hour
</verification>

<success_criteria>
Configuration reset to Option A and all three per-viewpoint feature files (>2MB each) are ready for training.
</success_criteria>

<output>
After completion, create `.planning/phases/05.7-data-pipeline-refactor/05.7-01-SUMMARY.md`
</output>
